Packages:
```
bluez-daemon asterisk-chan-lantiq asterisk-chan-mobile
```

## Bluetooth issues

```
Opcode 0x0c03 failed: -145
```

```bash
hciconfig hci0 down
hciconfig hci0 reset
hciconfig hci0 up
hciconfig hci0 voice 0x0060
```

Custom image packages:
```
base-files ca-bundle dnsmasq dropbear firewall4 fstools kmod-gpio-button-hotplug kmod-leds-gpio kmod-ltq-atm-vr9 kmod-ltq-deu-vr9 kmod-ltq-ptm-vr9 kmod-nft-offload libc libgcc libustream-mbedtls logd mtd netifd nftables odhcp6c odhcpd-ipv6only opkg ppp ppp-mod-pppoa ppp-mod-pppoe procd-ujail uci uclient-fetch urandom-seed urngd kmod-rt2800-pci wpad-basic-mbedtls kmod-usb-dwc2 kmod-ltq-tapi kmod-ltq-vmmc iwinfo luci bluez-daemon asterisk-chan-lantiq asterisk-chan-mobile
```
